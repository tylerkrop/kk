<div class="w-full">
  <div class="mb-2">
    <h1 class="text-2xl font-bold underline">Votes</h1>
  </div>
  <p>
    Vote for the books you want to read in the next meeting. You can drag and drop to reorder your votes.
  </p>
  <ul data-controller="sortable" class="mt-2 space-y-2">
    <% @votes.each do |vote| %>
      <li data-sortable-url="<%= vote_position_path(vote, vote.position) %>" class="bg-emerald-700 rounded-md flex items-center">
        <div data-sortable-handle class="w-full p-2 cursor-move">
          <%= vote.book.title %>
        </div>
        <div class="p-2">
          <%= button_to "Delete", vote_path(vote), method: :delete, data: { confirm: "Are you sure?" }, class: "bg-red-700 cursor-pointer hover:bg-red-600 font-semibold p-1 rounded-md" %>
        </div>
      </li>
    <% end %>
  </ul>
</div>
